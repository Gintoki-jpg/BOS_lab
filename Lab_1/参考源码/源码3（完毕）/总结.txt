源码3相对来说就比较新颖了，其基本原理和函数都是类似的，但是它是直接实现的线程库，然后将这两个编译完成的线程库放在linux可以找到的路径下让linux执行，所以在编译后产生的文件比较奇怪，同时makefile也比较奇怪，但是这还好，我们只需要参考他的函数源码即可；

context文件夹是上下文切换相关的函数以及相关测试，编译后生成mThread提供的动态库context/libcontext.so.1；
其他根目录下的文件是编译生成libmthread.so.1的源文件；
examples文件夹是用于测试两个动态库的测试程序；











